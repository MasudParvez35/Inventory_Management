using Microsoft.EntityFrameworkCore.Metadata.Builders;
using OA.Core.Domain;
using System.Security.Cryptography.X509Certificates;

namespace OA.Data.Mapping
{
    public class ProductMap
    {
        /*public ProductMap(EntityTypeBuilder<Product> builder)
        {
            //builder.HasKey(x => x.Id);
            //builder.Property(x => x.Name);
            //builder.HasOne(t => t.CategoryId).WithOne(u => Category).HasForeignKey<Category>(x => x.Id);
            ///builder.HasOne(x => x.CategoryId).WithOne(u => u.Category);
            //builder.Property(t => t.Email).IsRequired();
            //builder.Property(t => t.Password).IsRequired();
            //builder.HasOne(t => t.UserProfile).WithOne(u => u.User).HasForeignKey<UserProfile>(x => x.Id);
        }*/
    }
}